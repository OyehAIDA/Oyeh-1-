# Single-service Calculator (Next.js on Railway) — with sqrt + square

Operations: +, -, *, /, sqrt (√), x²

## Local
```bash
npm install
npm run dev
```

## Railway deploy
Push to GitHub → Railway deploy from repo.

This version pins Next.js to 14.2.35 to satisfy Railway security scanning.
